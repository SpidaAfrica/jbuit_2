# Base URL for the PHP backend API.
# Leave as-is for local dev (Vite proxies /backend -> http://localhost:8000).
# In production set this to your live API path, e.g.:
#   VITE_API_BASE=https://jbuit.com/backend/api
const API_BASE = "https://api.jbuit.org/api";
